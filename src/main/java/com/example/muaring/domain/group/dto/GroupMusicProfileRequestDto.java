package com.example.muaring.domain.group.dto;

import lombok.Getter;
import lombok.NoArgsConstructor;

@Getter
@NoArgsConstructor
public class GroupMusicProfileRequestDto {
    // 확장 여지용 (예: 나중에 기간 override, forceRecalculate 옵션 등)
    // 지금은 바디 없이도 사용 가능하게 비워둔다.
}
